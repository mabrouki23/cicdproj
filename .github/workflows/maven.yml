name: Build Full Stack Application

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  cleanup:
    runs-on: ubuntu-latest
    steps:
    - name: Cleanup previous artifacts
      uses: actions/github-script@v7
      with:
        script: |
          // Supprimer les anciens artifacts
          const artifacts = await github.rest.actions.listArtifactsForRepo({
            owner: context.repo.owner,
            repo: context.repo.repo,
          });
          
          for (const artifact of artifacts.data.artifacts) {
            if (artifact.name.startsWith('school-manager-')) {
              console.log(`Deleting artifact: ${artifact.name}`);
              await github.rest.actions.deleteArtifact({
                owner: context.repo.owner,
                repo: context.repo.repo,
                artifact_id: artifact.id,
              });
            }
          }
          console.log('✅ Previous artifacts cleaned up');

  build:
    runs-on: ubuntu-latest
    needs: cleanup
    
    services:
      mysql:
        image: mysql:8.0
        env:
          MYSQL_ALLOW_EMPTY_PASSWORD: yes
          MYSQL_DATABASE: schooldb
        ports:
          - 3306:3306
        options: --health-cmd="mysqladmin ping" --health-interval=10s --health-timeout=5s --health-retries=3

    steps:
    - name: Checkout code (fresh)
      uses: actions/checkout@v4
      
    - name: Clean build directories
      run: |
        echo "=== CLEANING BUILD DIRECTORIES ==="
        cd school-manager-multi
        
        # Nettoyer les dossiers de build
        rm -rf frontend/dist
        rm -rf frontend/node_modules
        rm -rf backend/target
        rm -rf backend/src/main/resources/static
        
        echo "✅ Build directories cleaned"
      shell: bash

    - name: Setup Node.js for Angular
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'
        cache-dependency-path: 'school-manager-multi/frontend/package-lock.json'

    - name: Build Angular Frontend
      run: |
        echo "=== BUILDING ANGULAR FRONTEND ==="
        cd school-manager-multi/frontend
        
        # Installation propre des dépendances
        npm ci --no-cache
        
        # Build Angular
        npm run build
        
        # Analyse de la structure
        echo "📁 Structure du build Angular:"
        find dist/ -type f | head -20
        echo "🔍 Détail du dossier dist:"
        ls -la dist/
      shell: bash

    - name: Copy Angular to Spring Boot
      run: |
        echo "=== COPYING ANGULAR TO SPRING BOOT ==="
        cd school-manager-multi
        
        # Préparer le dossier static
        rm -rf backend/src/main/resources/static
        mkdir -p backend/src/main/resources/static
        
        # Détecter et copier la structure Angular
        if [ -d "frontend/dist/frontend" ] && [ -f "frontend/dist/frontend/index.html" ]; then
          echo "📥 Copie depuis: frontend/dist/frontend/"
          cp -r frontend/dist/frontend/* backend/src/main/resources/static/
        elif [ -d "frontend/dist" ] && [ -f "frontend/dist/index.html" ]; then
          echo "📥 Copie depuis: frontend/dist/"
          cp -r frontend/dist/* backend/src/main/resources/static/
        else
          echo "🔍 Recherche de index.html..."
          INDEX_PATH=$(find frontend/dist -name "index.html" | head -1)
          if [ -n "$INDEX_PATH" ]; then
            INDEX_DIR=$(dirname "$INDEX_PATH")
            echo "📥 Copie depuis: $INDEX_DIR"
            cp -r "$INDEX_DIR"/* backend/src/main/resources/static/
          else
            echo "❌ Aucun index.html trouvé dans le build Angular!"
            exit 1
          fi
        fi
        
        # Vérification finale
        echo "✅ Copie terminée"
        echo "📁 Contenu du dossier static:"
        ls -la backend/src/main/resources/static/
        echo "🔍 index.html présent:"
        ls -la backend/src/main/resources/static/index.html
      shell: bash

    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        cache: 'maven'

    - name: Build Spring Boot Backend
      run: |
        echo "=== BUILDING SPRING BOOT BACKEND ==="
        cd school-manager-multi
        
        # Nettoyer et rebuild Maven
        mvn clean package -pl backend -am -DskipTests=false
        
        echo "✅ Spring Boot build completed"
      env:
        SPRING_DATASOURCE_URL: jdbc:mysql://localhost:3306/schooldb
        SPRING_DATASOURCE_USERNAME: root
        SPRING_DATASOURCE_PASSWORD: ""
      shell: bash

    - name: Verify JAR Content
      run: |
        echo "=== VERIFYING JAR CONTENT ==="
        cd school-manager-multi/backend/target
        
        JAR_FILE=$(ls *.jar)
        echo "📦 JAR: $JAR_FILE"
        
        echo "🔍 Fichiers Angular dans le JAR:"
        jar tf $JAR_FILE | grep -E "BOOT-INF/classes/static/.*\.(html|js|css)" | head -15
        
        echo "✅ Vérification index.html:"
        if jar tf $JAR_FILE | grep -q "index.html"; then
          echo "🎉 INDEX.HTPRÉSENT DANS LE JAR!"
        else
          echo "❌ index.html MANQUANT"
          echo "Contenu static complet:"
          jar tf $JAR_FILE | grep "BOOT-INF/classes/static/"
        fi
      shell: bash

    - name: Upload Fresh JAR Artifact
      uses: actions/upload-artifact@v4
      with:
        name: school-manager-fullstack
        path: school-manager-multi/backend/target/*.jar
        retention-days: 1  # Garder seulement 1 jour pour éviter l'accumulation

    - name: Final Verification
      run: |
        echo " "
        echo "🎉 BUILD COMPLETED SUCCESSFULLY! 🎉"
        echo "==================================="
        echo "✅ Anciens artifacts nettoyés"
        echo "✅ Build directories nettoyés"
        echo "✅ Angular buildé avec succès"
        echo "✅ Fichiers copiés correctement"
        echo "✅ Spring Boot avec Angular intégré"
        echo "✅ JAR vérifié et prêt"
        echo "📦 Nouvel artifact: school-manager-fullstack"
        echo " "
      shell: bash
